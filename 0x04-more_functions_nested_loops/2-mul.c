#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multi
 * @y: the second number to multi with first
 * Return: the result
 */

int mul(int x, int y)
{
	return (x * y);
}
