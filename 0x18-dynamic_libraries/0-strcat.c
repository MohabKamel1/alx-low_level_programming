#include "main.h"
#include <string.h>

/**
 * _strcat - resets value of n to 98
 * @dest: input
 * @src: input
 * Return: void
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
